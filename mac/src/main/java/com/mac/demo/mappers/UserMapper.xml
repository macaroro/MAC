<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTO Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mac.demo.mappers.UserMapper">

	<insert id="add" parameterType="com.mac.demo.model.User">
		INSERT INTO user(nameMac,birthMac,uidMac,upwMac,phoneNumMac,emailMac,nickNameMac,genderMac,cityMac,townMac,villageMac,managerMac)
	    VALUES(#{nameMac},#{birthMac},#{uidMac},#{upwMac},#{phoneNumMac},#{emailMac},#{nickNameMac},#{genderMac},#{emailMac},#{cityMac},#{townMac},#{villageMac},#{managerMac})
	</insert>

	<select id="getList" resultType="com.mac.demo.model.User">
		SELECT * FROM user
	</select>
	
	<select id="getDetail" resultType="com.mac.demo.model.User">
		SELECT * FROM user WHERE uidMac=#{uidMac}
	</select>

	<update id="edit" parameterType="com.mac.demo.model.User">
		UPDATE user SET nameMac=#{nameMac}, birthMac=#{birthMac}, cityMac=#{cityMac}, townMac=#{townMac}, villageMac=#{villageMac}, 
		emailMac=#{emailMac}, genderMac=#{genderMac}, upwMac=#{upwMac} WHERE uidMac=#{uidMac}
	</update>
	
	<delete id="delete" parameterType="com.mac.demo.model.User">
		DELETE FROM user WHERE userMac=#{userMac}
	</delete>
	
	<select id="getiD" resultType="com.mac.demo.model.User" 
	                   parameterType="String">
		SELECT id FROM user WHERE idMac=#{uidMac}
	</select>

</mapper>